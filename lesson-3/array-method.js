/**
 * * Array Method
 * */

const names = ["Michael", "Hannah", "Johnas"];

// forEach :=> iteration / Looping
names.forEach((name) => console.log(name));

// Map :=> memodifikasi array
names.map((name) => console.log(`Mr/Mrs: ${name}`));
